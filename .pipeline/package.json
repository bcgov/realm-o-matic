{
  "name": "pipeline",
  "version": "1.0.0",
  "description": "This a pipeliene script",
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "build": "node build.js",
    "clean": "node clean.js",
    "deploy": "node deploy.js",
    "version": "echo \"node@$(node --version) ($(which node))\" && echo \"npm@$(npm --version) ($(which npm))\" && npm ls",
    "test:lint": "eslint --env node --ext .js .",
    "test:lintFix": "eslint --fix --env node --ext .js ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bcgov/ocp-sso.git"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "pipeline-cli": "git+https://github.com/BCDevOps/pipeline-cli.git#v1.1"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.3",
    "eslint": "^6.6.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-config-prettier": "^6.5.0",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-prettier": "^3.1.1",
    "prettier": "^1.18.2"
  }
}
